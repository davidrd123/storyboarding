# Process Notes — what's productive, what's not (running log)

## Verified productive
- **Entropy + ban-list**: 36 seeds, 36 distinct mechanisms, zero dedup loss.
  The anti-collapse machinery works.
- **Platform test**: T01's invariant survived 4 displaced mutations — real
  evidence discipline, and cheap.
- **External review loop**: Codex caught the lens-as-cage error; fixed live.
- **Culture store retrieval**: coincidence hits (Alice door, Metropolis
  lamination) produce seeds with built-in citable references — half of Ben's
  deliverable ("references we love").
- **Anchor-calibrated Gemini paintability check** (2026-07-11): both anchors
  ranked correctly (giant hand #1, silence beat #8) → judgments credible.
  Cheap, comparative, text-level. Adopt at Gate 1.

## Not productive / corrected
1. **All-displacement-from-seed-1 ("temperature 2 from the start")** —
   David's catch. Every seed was entropy-displaced from birth, so the pool
   has a high weirdness floor and NO legible trunk. This inverted the
   architecture's own sources: Mueller's mutation is a LAST-RESORT operator
   on existing plans; UCB demands a boring base reality; "first 10 are the
   obviousness tax" meant generate the obvious AND KEEP IT as trunk
   material — not skip it. Ben's own examples (giant hand, mango voltron)
   are mainstream-legible.
   **Fix — trunk-first protocol:** per marked space, batch 1 = the obvious
   answers, NO entropy (write what the room would pitch; capture + ban their
   mechanisms). Then displacement operates ON the best trunks. Mutation
   from a legible base, not weirdness from vacuum.
2. **Self-certified paintability** — claimed all 5 batch-8 seeds were
   single-frame; Gemini (anchors correct) says: 1 half-viable, 2 need
   sequences, 2 cannot be stills at all. Claude's frame-intuition is not
   trustworthy solo. **Fix:** every seed advancing past Gate 1 gets the
   comparative Gemini paintability pass (always with 2 known anchors —
   distrust the batch if anchors misrank). Treatments must declare
   single-still vs 2-frame; Ben's format tolerates short sequences but
   they must be sold as such.
3. **Membrane lens as generation cage** — fixed earlier (lens roster).
4. **Seed pool skews wit/concept over spectacle** — Ben's axes are
   spectacle axes (scale, flying, world). Wit is a garnish here, not the
   dish. Trunk-first should also correct this, since the obvious answers
   in this genre ARE spectacle.

## Cross-agent render comparison (Codex batch-02 vs Fable deck-01, 2026-07-11)

- Two independent render batches now cover all four questions: Fable deck-01
  (NB Pro 2K: giant hand + roof edit, ribbon, triptych, peels-fall-up) and
  Codex batch-02 (NB2 Flash 1K: giant hand baseline, gummy canopy, multi-
  flavor mecha, giant-Raul-eats-Raul, two-world anchor, peel-film redraw).
- Cross-model verdicts begin: the peel VERB is hard in both models (Flash →
  disassembly; Pro → surface curl without open cabin); material continuity
  passed in Pro (v02 ribbon); large gummy membranes drift fleshy in Flash
  (tone warning now in product-facts.md).
- Adopted from Codex's write-up: visible-verb evaluation, premise/causality
  as independent axes, probe-cheap/finalize-expensive tiering — and its
  notes format (immediate read / works / doesn't yet / next test) as the
  house style. No numeric self-ratings anywhere (David's correction).

## Standing process (updated)
Per marked space: TRUNK batch (obvious, no entropy) → Gemini paintability
pass → mutate best trunks (entropy ON a legible base) → bridge → gates.
