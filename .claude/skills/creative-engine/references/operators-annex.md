# Operator Annex — the researched long tail

Promotable to core ONLY when session traces reveal a pathology the core 8
can't address. Full protocols live in the research corpus
(`creative-engine/research/01–05*.md` in the Storyboarding repo); this is
the index. Several annex moves are *harness patterns* (orchestration
shapes), not operators — marked (H).

## From Eno / chance-operation lineage (research/01)

| Move | One-line protocol | Absorbed into core? |
|---|---|---|
| Secret Cards (H) | deal each parallel subagent a different hidden card; harvest outputs as seeds, not products | — harness pattern |
| Error Adoption | before fixing any artifact/hallucination/wrong render: "assume this was intended — what work is it the correct version of?" | Gate 3 policy (verification.md) |
| Manufactured Scenius (H) | 3–5 personas with different jobs in the ecology (maker/curator/theorist/fashionable); 2–3 appropriation rounds | — expensive mode |
| Plant a System | design seed + transformation rules + stop condition; execute mechanically; harvest what grew; never dig up the seed | — |
| Scheduled Surrender | declared CONTROL/SURRENDER phases; critique vocabulary banned inside surrender | partially = phase wall |
| Compose in the Material | operate on artifacts (shot lists, frames), not synopses; cut/reverse/splice the artifact itself | — |
| Aphorism Collision | deal a gnomic quote; write the bridge "if this were secretly about my problem…" before the idea | — |
| Suit-Structured Deck | randomize intervention TYPE (look/ask/try/break), then content | — |

## From formal systems (research/02)

| Move | One-line protocol | Notes |
|---|---|---|
| Provocation ("po") + movement | deliberately false statement, then 5 mandatory movement operators (extract principle / difference / moment-to-moment / positives / special circumstances) | movement = BRIDGE's engine |
| SCAMPER all-7 | run ALL seven operators in order, 3 ideas each, no skipping | explicit-mutation covers the spirit |
| Concept fan | idea → concept behind it → alternative ideas → broader concept → fan again | platform extraction cousin |
| Challenge | non-hostile "why is it done this way at all?" on a non-problem | |
| TRIZ contradiction | "improving X worsens Y" + `entropy.py draw triz-principles -n 3` + no-trade-off rule | deck already shipped |
| Register retell | `entropy.py draw registers` — retell the fixed beat in the drawn register | deck already shipped |

## From artists/performers (research/03)

| Move | One-line protocol | Notes |
|---|---|---|
| Cut-up (Bowie pool) | seed a multi-domain fragment pool deliberately, `entropy.py shuffle`, curate collisions after | |
| Exquisite corpse (H) | sequential subagents, each seeing only the last line — the fold is context isolation | single context cannot play it |
| Automatic writing | 200-word uncensored sprint from a trigger word; mine in a separate pass | |
| Irrational enlargement | `entropy.py draw irrational-questions` — answer confidently, build from the answer | deck already shipped |
| Paranoiac-critical | over-read the product until it IS something else; keep both readings true in the final frame | |
| Status flip | replay the beat with status reversed (product high / person low, or inverse) | free deterministic transform |
| Benign-violation dial | rate violation 1–10; retune to 4–6 by stakes/cuteness/distance, never by removing the rule | folded into heightening |

## From advertising/VFX craft (research/04 — the 20-operator table)

Absorbed into core: two-pile collision (→ morphological), truest-thing +
flip-the-fence (→ reframe), one-rule ladder + two dials + cost (→
consequence-heightening), cliché blacklist (→ Gate 0).

Still annex: words-first (≤12-word stunning line, image derived from it) ·
category zig-list inversion · attribute→extreme-conclusion · feeling-transfer
(be the feeling, product absent till endframe) · claim→child-logic
proof-event · technique-first (invent the impossible camera trick, then its
story) · consumption-timeline→plot · swap-exactly-one-channel · 15-second
compression · platform extraction (in verification.md as a Gate 2 test) ·
dead-idea inventory (in postures.md as RECOVERY).
