---
name: session-handoff-2026-05-24
description: "Blind-pickup handoff for the 2026-05-24 session: full state, what happened, lessons, file map, and the exact next repair. Read this first to resume."
type: session_log
status: handoff
---

# Session Handoff — 2026-05-24

> **Purpose:** resume blind. Read §1 (state) + §6 (next step) first; §2–5 are context/lessons. Durable facts also live in agent memory; per-generation detail in the `prompts-*-active` breadcrumbs and `LOG.md`.

## 0. What this project is (30s)
A **storyboard pipeline** (`../../STORYBOARD_SYSTEM_BRIEF.md`): the system *executes a director's story* in a chosen art style with character/location continuity. The Greek-god scenes are **test beds**, not the product. This session built and stress-tested the full chain on one scene (**Hephaestus's forge**) and wrote a story layer for it. Image models driven via riff-mcp's `gemini-video-prompts` CLI (Gemini/NB models) and `scripts/gen_openai.py` (GPT-Image-2); video via `scripts/gen_seedance.py` (Seedance 2.0 on Replicate); analysis via `scripts/style_probe.py` (images) and the **new** `scripts/media_probe.py` (video+image).

## 1. CURRENT STATE (read first)

**Active scene:** Hephaestus "Gods in the Machine," Beat 1 (the forge). Story: `../story/hephaestus-gods-in-the-machine.md` (tragic-fable register; logline = the smith forges the world's unspent futures into golden bodies for Time to devour; the recognition fork — woken-one-is-a-stranger vs. is-*her* — is still **open**).

**Where the visuals are:**
- **Coverage board (clean, controlled, best):** the NB-Pro re-render `outputs/2026-05-24/gemini-3-pro-image-preview/01_hephaestus-9up-rerender-nbpro_31e4557a/…_01.png` — a clean 9-panel board (labels kept).
- **The 9-panel full-size KIT:** `outputs/2026-05-24/kit-nbpro/panel-01..09.png` (regenerated on NB Pro). Earlier kits: `kit-gpt/` (GPT), `kit-nb2anchor/` (NB2). The GPT kit is at `outputs/2026-05-24/gpt-image-2/01_hephaestus-kitNN-gpt_*`.

**🚨 THE PROBLEM that stopped us — the NB-Pro kit FAILS cross-panel QC.** Pretty per-panel, but **6/9 flagged** by a Gemini QC pass (`scripts/style_probe.py` over all 9; report archived in `LOG.md`/below):
- **Panel 1:** Hephaestus **duplicated** (sitting foreground AND standing mid-ground); foreground hand garbled.
- **Panel 3:** identity drift (young/heroic) — **REDONE** with an identity anchor → `…/01_hephaestus-kit03-nbpro-redo_b2120972/…_01.png` (now correctly weary/older; **use this, not kit-nbpro/panel-03**). *Caveat: redo skews quite old/white-haired — lock his exact age.*
- **Automaton design DRIFTS hard:** panel 5 = top-hat/crown head · 8 = C-3PO domed heads w/ ear-caps · 9 = blank mannequins · 3 = rounded heads + glowing eyes. The flat-topped cylindrical design from panels 1–2 doesn't hold.
- **Panel 4:** garbled hands (extra fingers) on the automaton's face.
- **Panel 8:** Hephaestus wardrobe drift (bare-chested vs draped robe).
- **Clean panels: 2, 6, 7.**

**QC COMPARISON (evidence, 2026-05-24): GPT kit = 1 flag vs NB-Pro kit = 6 flags** (identical Gemini QC). GPT held the automaton design ("exceptionally stable") + Hephaestus identity across 9 *separate* generations WITHOUT an asset-lock; NB-Pro drifted. **So: GPT-Image-2 needs far less hand-holding for cross-panel consistency** — tempering the earlier "NB Pro = best" call. Real trade = **consistency-ease (GPT) vs clean-surface (NB, but only with the asset-lock discipline).** Caveats: GPT's panel 4 still has a **3-arm/fused-finger anatomy glitch** (human-caught); and **the Gemini QC UNDER-called it** (flagged "fused fingers," missed the extra arm) — auto-QC is a net, not complete; keep a human eye.

**CONTENT-LEVEL read (David, 2026-05-24):** *on the whole the scene content got very close* — the schema-derived coverage + generation got most panels right. Only **~3 NB-Pro panels are "way off"** on content (David's eye; likely the automaton-design divergences — panel 5 top-hat head, 8 dome-heads+bare-chest, 9 blank mannequins — and/or panel 1's duplicate; **confirm which 3 next session**). **Takeaway: the pipeline is SOUND — this is a localized few-panel repair + asset-lock problem, not a rethink.** Encouraging: the approach works; fix the few + lock assets.

**ROOT CAUSE (of the NB-Pro drift):** we generated each panel from prompt + board but **skipped the Phase-0 asset/character LOCK** — no canonical automaton-design reference and no Hephaestus identity reference fed to every panel — so each panel *reinvented* the automaton head and let Hephaestus drift. (Our own `../patterns/coverage-discovery-schema.md` has this step; we didn't apply it to kit gen.) **Lesson burned in: pretty-per-panel ≠ consistent-across-panels; QC the whole set, don't trust a 3-frame glance.**

## 2. THE PIPELINE (validated this session)
`brief+bible → coverage-discovery schema (derive manifest) → one-shot board → per-panel full-frame kit → Seedance animatic`. All steps work; the kit step needs the asset-lock fix (§6).

**Model roles (validated — see `project_nb2-facture-ceiling` memory):**
- **NB2** `gemini-3.1-flash-image-preview` — fast/clean, weaker control. Fast volume.
- **GPT-Image-2** `gpt-image-2` (via `gen_openai.py`, no system slot → prepend) — strong control (reasons over prompts) but **gritty surface** (noise-as-feature; compounds when chained).
- **NB Pro** `gemini-3-pro-image-preview` — **control + clean** (reasons over the house grammar + multi-ref, renders clean). Best engine — BUT still drifts assets without a locked design ref (see QC).

**Animatic (Seedance) — RESOLVED methods:**
- Page as **first-frame** → animates panels in place ("living storyboard").
- Page/panels as **ingredient** + "one continuous take" → a **oner**.
- **Panels as named `[Image1..N]` ingredients prompted as ORDERED BEATS to transition through → a SEQUENCED, paced animatic in one generation** (the winner; Patrick-proven; corrects my earlier "Seedance freelances order" over-hedge).
- Per-panel **img2vid 5s clips → cut in an editor** = full control, mechanical timing.
- Best video so far: `outputs/2026-05-24/seedance-2.0/01_hephaestus-panels-as-ingredients_eff12bbd/` (ordered-beat). 3 individual 5s clips: `…shot0{1,2,3}-5s_*`.

## 3. WHAT HAPPENED (arc)
1. New MJ batch `MidJourney/2026-05-23/` (~10 hands) cataloged. Branched style-control probe from search-line to **hard styles**.
2. **Goya-dark impasto** (Saturn anchor) → parked ("looks terrible"; facture didn't transfer).
3. **Litho-crayon duotone** (Cerberus anchor) → facture *ceiling* on NB2 via description; fixed by **show-don't-tell crops + dropping artist names** → NB2 **82**, GPT **85**. (See `prompts-litho-active.md`, `nb2-facture-ceiling`.)
4. **Coverage probe:** works off a generated master, off raw MJ source (stronger), and across style families. Flipped fence = "preserve world, change camera."
5. Read **Patrick's vault** (`/Users/daviddickinson/Projects/Lora/ComfyPromptByAPI-patrick/WorkingSpace/patrick/vault_gml`) + **John's schema** (`../conversation/john/schema.txt`) → authored **`../patterns/coverage-discovery-schema.md`** (brief+bible → derived manifest; validated end-to-end, incl. the 180/eyeline rule).
6. **Story throughline** authored (`../story/hephaestus-gods-in-the-machine.md`, tragic fable). Gemini story-dev pass kept: brace-tethered lameness, silenced handmaidens, waking-by-touch, Fates-as-QC, unbolt-brace climax.
7. **Prompting got grounded** (David: "are you following the protocol or free-balling?"): the **house grammar** (`Preserve/Borrow/Do NOT borrow` per ref) is from `../../deep-research/2026-05-19/reply01.md` §5–6; prompts **externalized + logged before firing** (`prompts-hephaestus-kit-active.md`, `outputs/2026-05-24/kit-prompts/panel-NN.txt`).
8. **Noise-as-feature** found: GPT grain read as texture in chained upscales → fixed via grammar (`Do NOT borrow: grain`) + clean source anchor. Crop-only NB2 expansions drift; **shared source anchor** fixes it.
9. **GPT kit → NB2 launder → NB-Pro re-render** → landed on **NB Pro as control+clean**. Regenerated kit on NB Pro → **QC FAIL (§1).**
10. Built **`scripts/media_probe.py`** (mixed video+image → Gemini); verified shot-01's camera move ("dollies past him into the machines" — yes).

## 4. LESSONS (durable; also in memory)
- **Don't over-conclude from thin evidence** (`feedback_dont-over-conclude`) — I did it ≥3× (facture "structural ceiling"; Seedance "freelances order"; kit "consistent"). State open / get the artifact / replicate-and-observe. **QC catches what the eye misses.**
- **Asset/character LOCK before kit generation** (NEW this session) — without canonical design refs, panels drift. This is the Phase-0 step; apply it.
- **House grammar, externalized & logged** — assemble prompts from `Preserve/Borrow/Do NOT borrow`; don't free-ball; log before firing (Patrick discipline).
- **Show > tell** (crop refs); **noise-as-feature** (instruct "treat grain as artifact"); **shared style anchor** for kit consistency.
- **Model choice:** NB Pro = control+clean; NB2 = fast volume; GPT = control-but-gritty.
- **Seedance:** sequences ordered-beat ingredients; cleaner inputs translate better.
- **Calibrate with Gemini, comparative** (`feedback_style-classification-via-gemini`); open-ended `describe` > structured for recipes; `media_probe.py` for video QC.

## 5. FILE MAP
- **Story:** `../story/hephaestus-gods-in-the-machine.md`
- **Schema (the engine):** `../patterns/coverage-discovery-schema.md`
- **Coverage demo (schema run on Beats 2 & 7):** `../style-control-probe/coverage-demo-hephaestus.md`
- **Kit prompts (externalized):** `prompts-hephaestus-kit-active.md` + `outputs/2026-05-24/kit-prompts/panel-NN.txt`
- **Running lab log:** `LOG.md` · **Coverage breadcrumb:** `prompts-coverage-active.md` · **Litho:** `prompts-litho-active.md`
- **Kits:** `outputs/2026-05-24/kit-nbpro/` (current, QC-failed), `kit-gpt/`, `kit-nb2anchor/`; panel crops `outputs/2026-05-24/9up-panels/`
- **Clean board:** `outputs/2026-05-24/gemini-3-pro-image-preview/01_hephaestus-9up-rerender-nbpro_31e4557a/`
- **Scripts:** `../scripts/{gen_openai,gen_seedance,style_probe,media_probe,litho_post}.py`
- **Research:** `../deep-research/2026-05-19/reply01.md` (§5 grammar, §6 compositional skeleton)
- **Patrick vault:** `/Users/daviddickinson/Projects/Lora/ComfyPromptByAPI-patrick/WorkingSpace/patrick/vault_gml` · **John:** `../conversation/john/schema.txt`
- **Memory:** `project_nb2-facture-ceiling`, `project_scene-coverage-from-source`, `project_coverage-discovery-schema`, `feedback_dont-over-conclude`, `feedback_style-classification-via-gemini`, `reference_riff-mcp-analyze`.

## 6. NEXT STEP (the repair — do this first)
1. **Lock a canonical AUTOMATON design** (one reference image — the flat-topped cylindrical-head from kit-nbpro panel-02 is the cleanest; crop or use as-is) **and a Hephaestus IDENTITY reference** (decide his exact age — the panel-03 redo skewed too old; pick a target). Add both to the story bible.
2. **Regenerate the flagged panels (1, 3✓, 4, 5, 8, 9)** on NB Pro, feeding **[source style + canonical automaton ref + Hephaestus identity ref + clean board]** with house-grammar roles, so the design is locked. (Panel 3 already redone — keep `kit03-nbpro-redo`.)
3. **Re-run the Gemini QC** (`style_probe.py` over the new 9) until clean.
4. **Then** run the ordered-beat Seedance animatic on the clean kit (the step we were about to do).
5. **Retro-fix the schema — ✅ DONE:** `../patterns/coverage-discovery-schema.md` now has a **"Phase 0 — ASSET-LOCK before kit generation"** section (lock canonical character/asset refs, feed to every panel; QC the whole set + keep a human eye; camera/cutting is a separate layer).
6. **Owed cleanup (rigor items, not yet done):** (a) calibrated `compare_images` re-grade of the litho freehand numbers (75/78/82/85 are unanchored `analyze_image` guesses); (b) NB delivery-channel control (block as system vs prepended-to-prompt — see `LOG.md` rigor items); (c) the **story recognition fork** (woken-one = stranger vs = *her*, `story/…`); (d) confirm WHICH 3 NB-Pro panels David meant by "way off."

## 6b. NEXT CONCEPTUAL THREAD — the camera/cutting layer (David, 2026-05-24)
The 14s animatic is "not bad," but David's key insight: **the storyboard specifies the SHOTS (the "what" — composition/content) and leaves CAMERA EXECUTION + CUTTING (the "how" — moves, transitions, cut points, durations) open to interpretation** — so Seedance improvised the camera/editing. Coverage and cinematography/editing are two different layers; we only built the first. **Next: extend `coverage-discovery-schema.md` with a camera-execution + editing layer** — per shot a *Director-Mode camera command* (push-in/low-angle/rack/static), between shots a cut/transition type + a duration. **Patrick's `scene-coverage-protocol-v2.md` "Shot Type Vocabulary" table already has per-shot Director-Mode commands** — start there. This is where real camera/editing expertise (a DP/editor) adds the most value; the model will interpret if you don't direct it.

## 7. POST-HANDOFF — 14s full-sequence result (logged after David stepped away)
Fired the NB-Pro 9-up → Seedance at **14s** with all 9 beats + transitions scripted + "hold same smith/automaton design." **Best animatic of the session.** Threaded the arc IN ORDER with cinematic pacing: wide establish (0s) → smith lifts/studies a gold automaton (3s) → hands cradle its face, eyes waking (6s) → standing ranks (9s) → final wide, small among the silent ranks in a light shaft (13.8s). Character held (correct weary older Hephaestus), design reasonably consistent, **clean** (NB-Pro source). Confirms: **longer runtime + explicit beats/transitions + clean board → a fuller, ordered, paced sequence in one generation.** Output: `outputs/2026-05-24/seedance-2.0/01_hephaestus-9up-14s-full-sequence_f1099f85/`. → Strong evidence the animatic step is solid once the kit's asset-lock is fixed (§6).
